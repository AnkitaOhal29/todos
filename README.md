### How to Start
1. Install the Python 3.11
2. Create a virtual environment
3. Clone the repository `git clone https://github.com/AnkitaOhal29/todo.git`
4. Install all the dependencies by running the command `pip install -r requirements.txt`
5. run python manage.py makemigration & python manage.py migrate
6. run project python manage.py runserver
